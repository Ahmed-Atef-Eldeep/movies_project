class Data {
  String? type;

  Data({this.type});

  // factory Data.fromJson(Map<String, dynamic> json) => Data(
  //       type: json['type'] as String?,
  //     );

  // Map<String, dynamic> toJson() => {
  //       'type': type,
  //     };
}
